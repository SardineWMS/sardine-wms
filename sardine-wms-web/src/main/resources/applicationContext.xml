<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="http://www.springframework.org/schema/beans" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis="http://mybatis.org/schema/mybatis-spring" xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="
           http://www.springframework.org/schema/beans   http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc  http://www.springframework.org/schema/mvc/spring-mvc.xsd  
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
       "
	default-autowire="byName">

	<beans:import resource="classpath:dao.xml" />
	<beans:import resource="classpath:service.xml" />

	<context:annotation-config />

	<!-- 自动扫描所有注解 注册为bean (启动注解) -->
	<context:component-scan base-package="com.hd123.sardine.wms.web">
	</context:component-scan>

	<bean name="redisUtil" class="com.hd123.sardine.wms.web.base.jedis.RedisUtil">
		<property name="address" value="${jedis.address:172.17.1.54}" />
		<property name="port" value="${jedis.port:6379}" />
		<property name="auth" value="${jedis.auth:sardine}" />
<!-- 		<property name="maxActive" value="${jedis.maxActive:100}" />
		<property name="maxIdle" value="${jedis.maxIdle:20}" />
		<property name="maxWait" value="${jedis.maxWait:10}" /> -->
		<property name="timeout" value="${jedis.timeout:60000}" />
	</bean>

	<mvc:annotation-driven>
		<mvc:message-converters register-defaults="true">
			<bean class="org.springframework.http.converter.StringHttpMessageConverter">
				<constructor-arg value="UTF-8" />
				<property name="supportedMediaTypes">
					<list>
						<value>text/plain;charset=UTF-8</value>
						<value>text/html;charset=UTF-8</value>
					</list>
				</property>
			</bean>
			<bean
				class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
			</bean>
		</mvc:message-converters>
	</mvc:annotation-driven>

	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<bean class="com.hd123.sardine.wms.web.base.ControllerInterceptor">
				<property name="uncheckUrls">
					<list>
						<value>/ia/authen/login</value>
						<value>/ia/authen/register</value>
					</list>
				</property>
			</bean>
		</mvc:interceptor>
	</mvc:interceptors>
</beans>